---
  - hosts: nextcloud
    vars_files:
      - 'vars/vault.yml'
    roles:
      - grog.package
      - geerlingguy.ntp
      - geerlingguy.docker
      - geerlingguy.pip
      - nextcloud
      - caddy_ansible.caddy_ansible